import Home from "./pages/Home";

// Root component — currently just renders the Home page.
// If more pages are added later, routing can be introduced here.
function App() {
  return <Home />;
}

export default App;
